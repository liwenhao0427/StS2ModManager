<Window x:Class="StS2ModManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:StS2ModManager.ViewModels"
        mc:Ignorable="d"
        Title="杀戮尖塔2 Mod管理器" Height="750" Width="950"
        MinHeight="650" MinWidth="850"
        Background="#F5F5F5"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0C0C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="4,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="8,4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#7C3AED"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8E8E8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="🎮" FontSize="28" Margin="0,0,8,0"/>
            <TextBlock Text="杀戮尖塔2 Mod管理器" FontSize="24" FontWeight="Bold" Foreground="#7C3AED" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 游戏路径选择 -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="#DDDDDD" BorderThickness="1">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="游戏路径:" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Width="500" ItemsSource="{Binding DetectedPaths}" SelectedItem="{Binding SelectedPath}"/>
                    <Button Content="刷新" Command="{Binding RefreshPathsCommand}" Margin="8,0,0,0"/>
                    <Button Content="浏览..." Command="{Binding BrowseGamePathCommand}" Margin="4,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding GamePathStatus}" Foreground="#22C55E" FontSize="12"/>
                    <Button Content="📂 游戏目录" Command="{Binding OpenGameDirectoryCommand}" Margin="16,0,0,0" Padding="8,4"/>
                    <Button Content="📂 游戏Mods" Command="{Binding OpenGameModsDirectoryCommand}" Margin="4,0,0,0" Padding="8,4"/>
                    <Button Content="📂 工具Mods" Command="{Binding OpenToolModsDirectoryCommand}" Margin="4,0,0,0" Padding="8,4"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Mod管理 -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 工具Mods -->
            <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="8" Padding="12" BorderBrush="#DDDDDD" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="我的mod (工具目录)" FontSize="16" FontWeight="Bold"/>
                        <Button Content="🔄" Command="{Binding RefreshToolModsCommand}" ToolTip="刷新" Padding="6,2" Margin="8,0,0,0"/>
                    </StackPanel>
                    
                    <ListBox Grid.Row="1" ItemsSource="{Binding ToolMods}" Background="#FAFAFA" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsEnabled}" Margin="4,2">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}" Width="180" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding SizeDisplay}" Foreground="#666666" FontSize="11" Margin="8,0,0,0"/>
                                    </StackPanel>
                                </CheckBox>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="全选" Command="{Binding SelectAllModsCommand}" Padding="8,4"/>
                        <Button Content="取消" Command="{Binding DeselectAllModsCommand}" Padding="8,4"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 中间按钮 -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8">
                <Button Content="应用 →" Command="{Binding ApplyModsCommand}" FontWeight="Bold" Background="#7C3AED" Foreground="White"/>
                <Button Content="← 取出" Command="{Binding RemoveModsCommand}" FontWeight="Bold" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- 游戏Mods -->
            <Border Grid.Column="2" Background="#FFFFFF" CornerRadius="8" Padding="12" BorderBrush="#DDDDDD" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="游戏mod (游戏目录)" FontSize="16" FontWeight="Bold"/>
                        <Button Content="🔄" Command="{Binding RefreshGameModsCommand}" ToolTip="刷新" Padding="6,2" Margin="8,0,0,0"/>
                    </StackPanel>
                    
                    <ListBox Grid.Row="1" ItemsSource="{Binding GameMods}" Background="#FAFAFA" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✓" Foreground="#22C55E" Margin="4,0"/>
                                    <TextBlock Text="{Binding FileName}" Width="180" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding SizeDisplay}" Foreground="#666666" FontSize="11" Margin="8,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- 存档管理 -->
        <Border Grid.Row="3" Background="#FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="#DDDDDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="存档复制" FontSize="16" FontWeight="Bold"/>
                    <Button Content="📂 打开存档目录" Command="{Binding OpenSavesDirectoryCommand}" Margin="16,0,0,0"/>
                    <Button Content="📂 打开备份目录" Command="{Binding OpenBackupDirectoryCommand}" Margin="8,0,0,0"/>
                    <Button Content="🔄 刷新" Command="{Binding RefreshSteamIdsCommand}" Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="源Steam ID:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding SteamIds}" SelectedItem="{Binding SelectedSourceId}" Width="180"/>
                    <TextBlock Text="→" VerticalAlignment="Center" Margin="16,0"/>
                    <TextBlock Text="目标Steam ID:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding SteamIds}" SelectedItem="{Binding SelectedDestId}" Width="180"/>
                    <Button Content="复制存档" Command="{Binding CopySaveToAnotherCommand}" Margin="16,0,0,0" Background="#7C3AED" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 游戏启动 -->
        <Border Grid.Row="4" Background="#FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,16" BorderBrush="#DDDDDD" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="启动游戏" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <WrapPanel>
                    <Button Content="🎮 直接启动" Command="{Binding LaunchGameCommand}" Padding="16,8"/>
                    <Button Content="🎮 直接启动(无Mod)" Command="{Binding LaunchGameNoModsCommand}" Padding="16,8" Margin="8,0,0,0"/>
                    <Button Content="🔵 Steam启动" Command="{Binding LaunchGameViaSteamCommand}" Padding="16,8" Margin="16,0,0,0"/>
                    <Button Content="🔵 Steam启动(无Mod)" Command="{Binding LaunchGameViaSteamNoModsCommand}" Padding="16,8" Margin="8,0,0,0"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- 状态栏 -->
        <Border Grid.Row="5" Background="#FFFFFF" CornerRadius="4" Padding="12,8" BorderBrush="#DDDDDD" BorderThickness="1">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#666666"/>
        </Border>
    </Grid>
</Window>
