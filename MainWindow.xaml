<Window x:Class="StS2ModManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="杀戮尖塔2 Mod管理器"
        Width="1080"
        Height="700"
        MinWidth="920"
        MinHeight="600"
        MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}"
        MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}"
        FontFamily="Bahnschrift"
        Background="#EEF3F8"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <CollectionViewSource x:Key="GroupedToolMods" Source="{Binding ToolMods}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="SourceName"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#D7E1ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#F4F8FC"/>
            <Setter Property="BorderBrush" Value="#C9D7E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#1D78C9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1765AA"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#C9D7E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#C9D7E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="#D7E1ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FBFDFF"/>
        </Style>
    </Window.Resources>

    <TabControl Margin="10" TabStripPlacement="Left">
        <TabItem Header="主页">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <WrapPanel Grid.Row="0">
                            <TextBlock Text="游戏路径:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox Width="580" ItemsSource="{Binding DetectedPaths}" SelectedItem="{Binding SelectedPath}"/>
                            <Button Content="刷新" Command="{Binding RefreshPathsCommand}" Margin="8,0,0,0"/>
                            <Button Content="浏览..." Command="{Binding BrowseGamePathCommand}"/>
                        </WrapPanel>
                        <WrapPanel Grid.Row="1" Margin="0,8,0,0">
                            <TextBlock Text="{Binding GamePathStatus}" Foreground="#15803D" FontWeight="Bold"/>
                            <Button Content="游戏目录" Command="{Binding OpenGameDirectoryCommand}" Margin="12,0,0,0"/>
                            <Button Content="游戏Mods" Command="{Binding OpenGameModsDirectoryCommand}"/>
                            <Button Content="待生效" Command="{Binding OpenPendingModsDirectoryCommand}"/>
                        </WrapPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,8">
                    <WrapPanel>
                        <TextBlock Text="启动:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button Content="Steam启动" Command="{Binding LaunchGameViaSteamCommand}"/>
                        <Button Content="Steam无Mod" Command="{Binding LaunchGameViaSteamNoModsCommand}"/>
                        <Button Content="直接启动" Command="{Binding LaunchGameCommand}" Margin="12,0,0,0"/>
                        <Button Content="直接无Mod" Command="{Binding LaunchGameNoModsCommand}"/>
                    </WrapPanel>
                </Border>

                <Grid Grid.Row="2" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,4,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <WrapPanel Grid.Row="0">
                                <TextBlock Text="可用Mod（勾选生效）" FontWeight="Bold"/>
                                <Button Content="刷新" Command="{Binding RefreshToolModsCommand}" Margin="8,0,0,0" Padding="7,3"/>
                            </WrapPanel>
                            <ListBox Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding ToolMods}" SelectedItem="{Binding SelectedToolMod}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Command="{Binding DataContext.ToggleToolModCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      CommandParameter="{Binding}"/>
                                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="2" Text="{Binding VersionDisplay}" HorizontalAlignment="Center" Foreground="#5D7388"/>
                                            <TextBlock Grid.Column="3" Text="{Binding AuthorDisplay}" TextTrimming="CharacterEllipsis" Foreground="#5D7388"/>
                                            <Button Grid.Column="4" Content="打开" Command="{Binding DataContext.OpenModFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Padding="7,2"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="4,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <WrapPanel Grid.Row="0">
                                <TextBlock Text="生效Mod（取消勾选移出）" FontWeight="Bold"/>
                                <Button Content="刷新" Command="{Binding RefreshGameModsCommand}" Margin="8,0,0,0" Padding="7,3"/>
                            </WrapPanel>
                            <ListBox Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding GameMods}" SelectedItem="{Binding SelectedGameMod}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Command="{Binding DataContext.ToggleGameModCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      CommandParameter="{Binding}"/>
                                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="2" Text="{Binding VersionDisplay}" HorizontalAlignment="Center" Foreground="#5D7388"/>
                                            <TextBlock Grid.Column="3" Text="{Binding AuthorDisplay}" TextTrimming="CharacterEllipsis" Foreground="#5D7388"/>
                                            <Button Grid.Column="4" Content="打开" Command="{Binding DataContext.OpenModFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Padding="7,2"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>

                <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Padding="10,6">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#4B6073"/>
                </Border>
            </Grid>
        </TabItem>

        <TabItem Header="Mod管理">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,8">
                    <WrapPanel>
                        <Button Content="添加来源" Command="{Binding AddModSourceCommand}"/>
                        <Button Content="移除来源" Command="{Binding RemoveModSourceCommand}"/>
                        <Button Content="打开来源" Command="{Binding OpenSelectedModSourceCommand}"/>
                        <Button Content="全选生效" Command="{Binding SelectAllModsCommand}" Margin="12,0,0,0"/>
                        <Button Content="全部取消" Command="{Binding DeselectAllModsCommand}"/>
                        <Button Content="刷新全部" Command="{Binding RefreshToolModsCommand}"/>
                    </WrapPanel>
                </Border>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.15*"/>
                        <ColumnDefinition Width="1.15*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,4,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="可用Mod（按来源分组）" FontWeight="Bold"/>
                            <ListBox Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding Source={StaticResource GroupedToolMods}}" SelectedItem="{Binding SelectedToolMod}">
                                <ListBox.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.HeaderTemplate>
                                            <DataTemplate>
                                                <Border Background="#EEF4FB" Padding="6,3" Margin="0,6,0,0" CornerRadius="4">
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                </Border>
                                            </DataTemplate>
                                        </GroupStyle.HeaderTemplate>
                                    </GroupStyle>
                                </ListBox.GroupStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="65"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Command="{Binding DataContext.ToggleToolModCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      CommandParameter="{Binding}"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding AuthorDisplay}" FontSize="11" Foreground="#5D7388" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding UpdatedDisplay}" FontSize="11" Foreground="#5D7388" VerticalAlignment="Center"/>
                                            <Button Grid.Column="3" Content="打开" Command="{Binding DataContext.OpenModFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Padding="7,2"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="4,0,4,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="生效Mod" FontWeight="Bold"/>
                            <ListBox Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding GameMods}" SelectedItem="{Binding SelectedGameMod}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="65"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Command="{Binding DataContext.ToggleGameModCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      CommandParameter="{Binding}"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding AuthorDisplay}" FontSize="11" Foreground="#5D7388" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding UpdatedDisplay}" FontSize="11" Foreground="#5D7388" VerticalAlignment="Center"/>
                                            <Button Grid.Column="3" Content="打开" Command="{Binding DataContext.OpenModFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Padding="7,2"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Margin="4,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Mod详情（可编辑）" FontWeight="Bold"/>

                            <Grid Grid.Row="1" Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="名称:" Margin="0,0,8,6" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ModNameInput, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="版本:" Margin="0,0,8,6" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ModVersionInput, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="作者:" Margin="0,0,8,6" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ModAuthorInput, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="下载链接:" Margin="0,0,8,6" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ModDownloadUrlInput, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="作者链接:" Margin="0,0,8,6" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,6" Text="{Binding ModAuthorUrlInput, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="社交链接:" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding ModSocialUrlInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>

                            <Grid Grid.Row="2" Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="备注:" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding ModRemarkInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>

                            <Grid Grid.Row="3" Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="详情:" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding ModDescriptionInput, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>

                            <WrapPanel Grid.Row="4" Margin="0,10,0,0">
                                <Button Content="保存" Command="{Binding SaveModMetaCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                                <Button Content="重置" Command="{Binding ClearModMetaCommand}"/>
                                <Button Content="下载页" Command="{Binding OpenUrlCommand}" CommandParameter="{Binding ModDownloadUrlInput}"/>
                                <Button Content="作者页" Command="{Binding OpenUrlCommand}" CommandParameter="{Binding ModAuthorUrlInput}"/>
                                <Button Content="社交页" Command="{Binding OpenUrlCommand}" CommandParameter="{Binding ModSocialUrlInput}"/>
                            </WrapPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
